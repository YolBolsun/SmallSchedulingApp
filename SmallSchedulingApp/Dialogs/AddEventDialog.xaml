<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="SmallSchedulingApp.Dialogs.AddEventDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add Event"
    PrimaryButtonText="Add"
    CloseButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <StackPanel Spacing="12">
        <TextBox x:Name="EventNameBox"
                 Header="Event Name"
                 PlaceholderText="Enter event name"/>

        <CalendarDatePicker x:Name="StartDatePicker"
                            Header="Start Date"
                            Date="{x:Bind DefaultDate, Mode=OneWay}"/>

        <ComboBox x:Name="FrequencyCombo"
                  Header="Frequency"
                  SelectedIndex="0"
                  HorizontalAlignment="Stretch">
            <ComboBoxItem Content="Daily"/>
            <ComboBoxItem Content="Weekly"/>
            <ComboBoxItem Content="Bi-weekly"/>
            <ComboBoxItem Content="Monthly"/>
        </ComboBox>

        <NumberBox x:Name="OccurrencesBox"
                   Header="Event Occurrences"
                   Value="1"
                   Minimum="1"
                   SpinButtonPlacementMode="Inline"
                   PlaceholderText="Number of times to repeat"/>
    </StackPanel>
</ContentDialog>
